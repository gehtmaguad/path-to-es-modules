- create bundle: `./node_modules/.bin/browserify main.js > bundle.js`

- open with browser, e.g. from the cmd with `chromium-browser index.html`
- open developer tabs, switch to console
- notice `console.log` output of script
